---
import type { Site } from '@schemas/Site'
import SiteTrayHeader from './SiteTrayHeader.astro'
import SitePageListNav from './SitePageListNav.astro'
interface Props {
  site: Site
}
const { site } = Astro.props
---

<SiteTrayHeader site={site} />
<SitePageListNav site={site} />
